{
  "version": 2,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.ts" },
    { "src": "/(.*)", "dest": "/client/dist/$1" }
  ],
  "builds": [
    { "src": "api/index.ts", "use": "@vercel/node" }
  ]
}
